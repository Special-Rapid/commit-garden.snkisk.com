import { usePreferences } from '../lib/preferences';

export function AppearanceControls() { const { locale, setLocale, theme, setTheme } = usePreferences(); return <div className="appearance-controls" aria-label={locale === 'ja' ? '表示設定' : 'Display preferences'}><button type="button" className="preference-button" aria-pressed={theme === 'dark'} onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}>{theme === 'dark' ? '☀' : '◐'} <span>{theme === 'dark' ? (locale === 'ja' ? 'ライト' : 'Light') : (locale === 'ja' ? 'ダーク' : 'Dark')}</span></button><button type="button" className="preference-button" onClick={() => setLocale(locale === 'en' ? 'ja' : 'en')} aria-label={locale === 'en' ? '日本語に切り替え' : 'Switch to English'}>{locale === 'en' ? '日本語' : 'EN'}</button></div>; }
