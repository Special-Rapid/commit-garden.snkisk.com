import { usePreferences } from '../lib/preferences';

export function AppearanceControls() { const { locale, setLocale, themePreference, setThemePreference } = usePreferences(); const ja = locale === 'ja'; return <div className="appearance-controls" aria-label={ja ? '表示設定' : 'Display preferences'}><div className="theme-options" role="group" aria-label={ja ? 'テーマ' : 'Theme'}>{(['system', 'light', 'dark'] as const).map((option) => <button key={option} type="button" className="preference-button" aria-pressed={themePreference === option} onClick={() => setThemePreference(option)}>{option === 'system' ? (ja ? '自動' : 'Auto') : option === 'light' ? (ja ? 'ライト' : 'Light') : (ja ? 'ダーク' : 'Dark')}</button>)}</div><button type="button" className="preference-button" onClick={() => setLocale(locale === 'en' ? 'ja' : 'en')} aria-label={locale === 'en' ? '日本語に切り替え' : 'Switch to English'}>{locale === 'en' ? '日本語' : 'EN'}</button></div>; }
